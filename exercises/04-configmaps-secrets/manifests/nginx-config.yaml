apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-config
  labels:
    exercise: "04"
data:
  default.conf: |
    server {
      listen 80;
      location / {
        return 200 'ConfigMap works!\n';
        add_header Content-Type text/plain;
      }
    }
