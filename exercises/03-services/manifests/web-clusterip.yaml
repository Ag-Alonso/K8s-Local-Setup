apiVersion: v1
kind: Service
metadata:
  name: web-clusterip
  labels:
    app: web
    exercise: "03"
spec:
  type: ClusterIP
  selector:
    app: web
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
