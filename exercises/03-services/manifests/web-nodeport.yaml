apiVersion: v1
kind: Service
metadata:
  name: web-nodeport
  labels:
    app: web
    exercise: "03"
spec:
  type: NodePort
  selector:
    app: web
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
      protocol: TCP
